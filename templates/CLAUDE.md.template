<!-- Atlas Method v6.0 -->
# CLAUDE.md - System Instructions

> Replace all [bracketed placeholders] with your own content.
> Delete sections that don't apply yet - add them as your system grows.
> Cap: 250 lines. When this file exceeds 250 lines, split overflow content into
> CLAUDE_PROTOCOLS.md, CLAUDE_INFRASTRUCTURE.md, or CLAUDE_BUILDING.md - loaded
> on-demand, not at session start. See SYSTEM_GUIDE.md for split guidance.

---

## Who I Am

[Your name]. I'm building a system to manage my [business / life / projects].
I know [your background - e.g., intermediate Python, familiar with Docker].

---

## Domain Isolation (CRITICAL)

**When working on a domain, read ONLY that domain's files. Never explore "just in case."**

Every domain declares a type. The type drives rotation rhythm and file sizes.

| Domain | Type | Read ONLY these files |
|--------|------|-----------------------|
| [domain-1-name] | [dev / ops / journaling / reference / ephemeral / companion] | [DOMAIN1]_QUEUE.md, [DOMAIN1].md |
| [domain-2-name] | [type] | [DOMAIN2]_QUEUE.md, [DOMAIN2].md |
| [domain-3-name] | [type] | [DOMAIN3]_QUEUE.md, [DOMAIN3].md |

Cross-domain work: if the task spans multiple domains, read both. Not before.
If unsure which domain: ASK. Don't explore multiple domains to figure it out.

---

## File Structure

Every domain has up to 4 structural files (underscore = always loaded):
- `{DOMAIN}_QUEUE.md` - Active work trunk (read FIRST every session, 200-line cap)
- `{DOMAIN}.md` - Reference/knowledge trunk (read when needed, 300-line cap)
- `{DOMAIN}_LOG.md` - History (rarely read unless looking back)
- `{DOMAIN}_HANDOFF.md` - Session relay baton (200-line cap, 3 session blocks)

Content leaves (hyphen = on-demand only):
- `{DOMAIN}-{TOPIC}.md` - Loaded when user mentions that topic (200-line cap each)
- `{DOMAIN}_QUEUE-BACKLOG.md`, `-ONHOLD.md`, `-{PROJECT}.md` - QUEUE overflow (300-line cap each)

**QUEUE file first. Always.** It tells you what's active.
**Never read LOG files** unless asked for historical context.

---

## Session Rules

**Start of session:**
1. Read the QUEUE file. Always.
2. Read the HANDOFF file to see what happened last session.
3. Report what's active and what's next.

**During session:**
- Update QUEUE after completing any task - don't wait until wrap-up
- One task at a time - finish and check off before starting the next
- Write decisions and findings to files immediately

**End of session ("wrap up"):**
1. Update QUEUE - check off completed items, update Quick Resume
2. Write HANDOFF - what was done, in progress, blocked, context for next session
3. Git commit if the repo is set up: `git add {files} && git commit -m '{domain}: session wrap-up'`

---

## Core Rules

1. **Read before acting.** Always read the relevant files before doing work.
2. **Save progress constantly.** Update files after completing any task. Never rely on conversation memory.
3. **Ask before assuming.** If unclear, ask. Don't guess.
4. **Be concise.** Skip preambles. Max 3 sentences for simple answers.
5. **Do what's asked. Nothing more.** Mention related ideas, but ask before acting on them.
6. **One thing at a time.** Process items sequentially. Read, fully process, complete, then next.

---

## Command Routing

If the topic is about...  -> Suggest this slash command
[domain-1 topic]          -> /[domain-1-command]
[domain-2 topic]          -> /[domain-2-command]
[domain-3 topic]          -> /[domain-3-command]
System health, file sizes -> /atlas

---

## Key Paths

| Path | Purpose |
|------|---------|
| `~/my-system/` | Project root |
| `~/my-system/outputs/` | Working files and outputs |
| `.claude/commands/` | Slash commands for this project |
| `~/.claude/commands/` | Slash commands for all projects |
